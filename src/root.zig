//! litem8 - SQLite migration tool library
//! This module is kept minimal as the main functionality is in the CLI.

const std = @import("std");

test "library tests" {
    // Library tests can be added here if needed
    std.testing.refAllDecls(@This());
}
